FROM golang:1.26-alpine AS build
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /kunn-client ./cmd/client

FROM alpine:3.20
RUN apk add --no-cache ca-certificates
COPY --from=build /kunn-client /usr/local/bin/kunn-client
ENTRYPOINT ["kunn-client"]
